---
import Layout from "../layouts/Layout.astro";

const projects = [
	{
		title: "whocan.work",
		description: "A public availability board so circles can see who’s free without juggling calendars.",
		stage: "Public access",
		availability: "Live today",
	},
	{
		title: "sharethis.site",
		description: "Single-word slugs that make passing links around effortless.",
		stage: "Public access",
		availability: "Live today",
	},
	{
		title: "Billie",
		description: "Invite-only receipt parsing and expense tracking for close collaborators.",
		stage: "Invite only",
		availability: "Request access",
	},
];

const stack = [
	{ label: "Convex", detail: "state + realtime data" },
	{ label: "Logto", detail: "auth + identity" },
	{ label: "Astro", detail: "static + islands" },
	{ label: "Cloudflare", detail: "edge + routing" },
];
---

<Layout title="Scrypted — dev console">
	<section id="hero" class="glass-panel grid gap-8 px-6 py-8 md:grid-cols-[minmax(0,1.8fr)_minmax(0,1fr)]">
		<div class="space-y-5">
			<p class="tag-pill">Personal Dev Stack</p>
			<h1 class="text-[clamp(2.4rem,4vw,3.3rem)] font-semibold leading-[1.15] text-black">
				Building thoughtful tools for my circle—shipping the good ones to the public soon.
			</h1>
			<p class="text-lg text-black/70 md:text-xl">
				Scrypted is where my experiments live. Friends and family use them first; when they're stable, the rest of the internet gets an invite.
			</p>
			<div class="flex flex-wrap gap-3 pt-2">
				<a
					href="#projects"
					class="inline-flex items-center justify-center rounded-full border border-black bg-black px-6 py-3 text-[0.78rem] font-semibold uppercase tracking-[0.18em] text-white transition-colors hover:bg-black/90"
				>
					Browse builds
				</a>
				<a
					href="#contact"
					class="inline-flex items-center justify-center rounded-full border border-black/50 px-6 py-3 text-[0.78rem] font-semibold uppercase tracking-[0.18em] text-black transition-colors hover:bg-black/5"
				>
					Request access
				</a>
			</div>
		</div>
		<div class="space-y-4 border-t border-black/10 pt-4 text-sm text-black/80 md:border-l md:border-t-0 md:pl-5 md:pt-0">
			<div class="space-y-1">
				<p class="text-[0.75rem] uppercase tracking-[0.2em] text-black/60">Current focus</p>
				<p class="leading-relaxed">Developer tooling for people I actually know.</p>
			</div>
			<div class="space-y-1">
				<p class="text-[0.75rem] uppercase tracking-[0.2em] text-black/60">Availability</p>
				<p class="leading-relaxed">Most apps private. Select launches announced soon.</p>
			</div>
		</div>
	</section>

	<section id="projects" class="space-y-5">
		<div class="space-y-3">
			<p class="tag-pill">Active builds</p>
			<h2 class="text-3xl font-semibold text-black md:text-[2.2rem]">Projects on rotation</h2>
			<p class="max-w-2xl text-black/70">Some apps are polished, some are barely holding together. That's the point.</p>
		</div>
		<div class="grid gap-4 sm:grid-cols-2 lg:grid-cols-3">
			{projects.map((project) => (
				<article class="grid-card flex flex-col gap-3">
					<header class="flex items-baseline justify-between gap-3">
						<h3 class="text-xl font-semibold text-black">{project.title}</h3>
						<span class="text-[0.7rem] uppercase tracking-[0.22em] text-black/60">{project.stage}</span>
					</header>
					<p class="text-sm text-black/70">{project.description}</p>
					<p class="text-sm font-semibold text-[#007d68]">{project.availability}</p>
				</article>
			))}
		</div>
	</section>

	<section id="stack" class="glass-panel grid gap-6 px-6 py-7 md:grid-cols-[minmax(0,1.2fr)_minmax(0,0.8fr)]">
		<div class="space-y-3">
			<p class="tag-pill">Under the hood</p>
			<h2 class="text-3xl font-semibold text-black">The stack that keeps us honest</h2>
			<p class="text-black/70">
				Convex and Logto handle the heavy lifting for authentication and realtime data. That means your Google-linked email and identity bits are stored securely—never shared, always delete-on-request.
			</p>
		</div>
		<ul class="space-y-4 text-sm text-black/80">
			{stack.map((item) => (
				<li class="border-b border-black/10 pb-3 last:border-none last:pb-0">
					<span class="block text-base font-semibold text-black">{item.label}</span>
					<p>{item.detail}</p>
				</li>
			))}
		</ul>
	</section>

	<section id="status" class="glass-panel grid gap-6 px-6 py-7 md:grid-cols-[minmax(0,1fr)_minmax(0,0.85fr)]">
		<div class="space-y-3">
			<h2 class="text-3xl font-semibold text-black">Reality check</h2>
			<p class="text-black/70">
				Most products here are in "developer phase" which means things may reset, break, or disappear. Back up anything important. Ping me if you want out—accounts get removed manually.
			</p>
		</div>
		<ul class="list-disc space-y-2 pl-5 text-sm text-black/80">
			<li>Data stored via Convex</li>
			<li>Authentication powered by Logto + Google</li>
			<li>No data sharing with third parties</li>
			<li>Account deletion by email request only</li>
		</ul>
	</section>

	<section id="contact" class="glass-panel space-y-4 px-6 py-7">
		<p class="tag-pill">Get in touch</p>
		<h2 class="text-3xl font-semibold text-black">Want to test something?</h2>
		<p class="text-black/75">
			Send a note to <a class="underline decoration-black/40 underline-offset-4" href="mailto:hello@scrypted.dev">hello@scrypted.dev</a> with what you're chasing. I'll point you to the right build (or tell you it's not ready).
		</p>
	</section>
</Layout>
